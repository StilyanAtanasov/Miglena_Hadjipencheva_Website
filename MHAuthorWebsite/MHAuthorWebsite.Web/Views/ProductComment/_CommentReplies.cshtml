@using MHAuthorWebsite.Web.ViewModels.ProductComment
@using Microsoft.AspNetCore.Mvc.TagHelpers
@using MHAuthorWebsite.Core.Models.Enums
@using static MHAuthorWebsite.GCommon.ApplicationRules.Application

@model ICollection<ProductCommentReplyViewModel>

@{
	TimeZoneInfo defaultZone = TimeZoneInfo.FindSystemTimeZoneById(DefaultTimeZoneId);
}

@foreach (ProductCommentReplyViewModel reply in Model)
{
	<div class="comment reply flex-row">
		<div class="comment-info flex-column">
			@if (reply.IsWriterAdmin)
			{
				<p class="role-badge flex-row">Администратор</p>
			}
			<div class="flex-row">
				<i class="fa-solid fa-reply"></i>
				<p>@reply.UserName</p>
			</div>
			<div class="dates flex-row">
				<p class="date">
					<time to-local date-only datetime="@reply.Date.ToString("O")">
						@TimeZoneInfo.ConvertTimeFromUtc(reply.Date, defaultZone).ToString("dd.MM.yyyy")
					</time>
				</p>
				@if (reply.LastEdited.HasValue)
				{
					<span class="edited-date" title="Последно редактиран на @TimeZoneInfo.ConvertTimeFromUtc(reply.LastEdited.Value.Date, defaultZone).ToString("dd.MM.yyyy")">
						<i class="fa-solid fa-pen"></i>
					</span>
				}
			</div>
		</div>
		<hr class="vertical comment-divider" />
		<div class="comment-main">
			<div class="rating-box flex-row">
				@if (reply is { VerifiedPurchase: true, IsWriterAdmin: false })
				{
					<p class="verified-badge flex-row"><i class="fa-solid fa-badge-check"></i> Потвърдена поръчка!</p>
				}
			</div>
			@if (reply.ReplyCommentWriterName is not null)
			{
				<div class="reply-person flex-column">
					<p class="flex-row"><i class="fa-solid fa-user-message"></i> <em><strong>Re:</strong> @reply.ReplyCommentWriterName</em></p>
					<hr />
				</div>
			}
			<p class="text">@reply.Text</p>
			<div class="comment-actions flex-row">
				<div class="comment-reactions flex-row" data-comment-id="@reply.Id">
					<button class="react-btn btn-none link" data-reaction-type="@((int)CommentReaction.Like)">
						<i class="fa-solid @(reply.UserReaction == CommentReaction.Like ? "" : "regular") fa-thumbs-up"></i>
						<span class="reaction-count">@reply.Likes</span>
					</button>
					<button class="react-btn btn-none link" data-reaction-type="@((int)CommentReaction.Dislike)">
						<i class="fa-solid @(reply.UserReaction == CommentReaction.Dislike ? "" : "regular") fa-thumbs-down"></i>
						<span class="reaction-count">@reply.Dislikes</span>
					</button>
				</div>

				<div>
					<a class="reply-btn btn-none link" asp-controller="ProductComment" asp-action="AddComment"
					   asp-route-productId="@reply.ProductId" asp-route-parentCommentId="@reply.ParentCommentId"
					   asp-route-replyCommentId="@reply.Id" asp-route-targetName="@reply.UserName" aria-label="Отговори на @reply.UserName">
						<i class="fa-solid regular fa-reply"></i>
					</a>
				</div>

				@if (reply.IsUserAuthor)
				{
					<div>
						<a class="btn-none link" asp-controller="ProductComment" asp-action="Edit"
						   asp-route-commentId="@reply.Id" aria-label="Редактирай коментар">
							<i class="fa-solid fa-pen"></i>
						</a>

						<button class="delete-btn btn-none link" data-comment-id="@reply.Id" data-product-id="@reply.ProductId" aria-label="Изтрий коментар">
							<i class="fa-solid fa-trash"></i>
						</button>
					</div>
				}
			</div>
		</div>
	</div>
}